{
  "name": "propcycle-firestore-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test firestore.rules.test.mjs",
    "test:rules": "firebase --config ../firebase.json emulators:exec --only firestore --project demo-propcycle \"npm test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.17.1",
    "firebase-tools": "15.26.0"
  }
}
